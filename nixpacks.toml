# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm ci --production"]
paths = ["backend"]

[phases.build]
cmds = ["echo 'No build step needed'"]

[start]
cmd = "npm start"
